name: CI workflow

on:
  push:
    branches:
      - develop
      - main

  pull_request:
    branches:
      - develop
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Java 21
        uses: actions/setup-java@v6
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Make Maven wrapper executable
        run: chmod +x backend/mvnw

      - name: Build and run unit tests with Maven
        run: cd backend && ./mvnw clean test -Dtest="!ExpenseManagerApplicationTests"   # Run only unit tests

      - name: Start the application
        env:
          POSTGRES_DB: ${{ vars.POSTGRES_DB }}
          POSTGRES_USER: ${{ vars.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.CI_POSTGRES_PASSWORD }}
          JWT_SECRET: ${{ secrets.CI_JWT_SECRET }}

        run: docker compose up --build -d    # Build before starting containers

      - name: Set up node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman collection
        env:
          BASE_URL: http://localhost:8080
        run: newman run postman/"Expense Manager API.postman_collection.json" --env-var "baseUrl=$BASE_URL"   # Run Postman API tests

      - name: Stop containers
        if: ${{ always() }}   # Always run this step
        run: docker compose down -v   # Stops and removes the containers, networks, volumes, images created